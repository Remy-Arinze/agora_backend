trigger:
  - develop

pool:
  name: 'SandboxAgent' # KEPT AS REQUESTED

variables:
  azureSubscription: 'agora-current-sc'
  apiAppName: 'agora-api-dev'
  webAppName: 'agora-web-dev'

stages:
# ==========================================================
# STAGE 1: INTELLIGENT CHANGE DETECTION (PowerShell Version)
# ==========================================================
- stage: CheckChanges
  displayName: 'Analyze Changed Paths'
  jobs:
  - job: Check
    displayName: 'Check Git Diff'
    steps:
    - checkout: self
      fetchDepth: 0
    
    # CHANGED: Replaced 'bash' with 'powershell' to work on Windows Agent
    - powershell: |
        Write-Host "Checking for changes..."
        
        # Capture the git diff output into a variable
        $changes = git diff --name-only HEAD~1 HEAD
        
        # Check if apps/api folder was touched using regex matching
        if ($changes -match "^apps/api/") {
          Write-Host "Changes detected in apps/api"
          Write-Host "##vso[task.setvariable variable=apiChanged;isOutput=true]true"
        } else {
          Write-Host "No changes in apps/api"
          Write-Host "##vso[task.setvariable variable=apiChanged;isOutput=true]false"
        }
        
        # Check if apps/web folder was touched
        if ($changes -match "^apps/web/") {
          Write-Host "Changes detected in apps/web"
          Write-Host "##vso[task.setvariable variable=webChanged;isOutput=true]true"
        } else {
          Write-Host "No changes in apps/web"
          Write-Host "##vso[task.setvariable variable=webChanged;isOutput=true]false"
        }
      name: CheckStep

# ==========================================================
# STAGE 2: BACKEND DEPLOY (Apps/API)
# ==========================================================
- stage: Backend
  displayName: 'Deploy Backend (API)'
  dependsOn: CheckChanges
  condition: eq(dependencies.CheckChanges.outputs['Check.CheckStep.apiChanged'], 'true')
  variables:
    workingDir: 'apps/api'
  jobs:
  - job: BuildAndDeployAPI
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
    
    - script: npm ci
      workingDirectory: $(workingDir)
      displayName: 'Install API Dependencies'

    - script: npm run build
      workingDirectory: $(workingDir)
      displayName: 'Build NestJS'

    - script: npm prune --production
      workingDirectory: $(workingDir)
      displayName: 'Prune Dev Dependencies'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(workingDir)'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/api.zip'
        replaceExistingArchive: true

    - task: AzureWebApp@1
      inputs:
        azureSubscription: $(azureSubscription)
        appType: 'webAppLinux'
        appName: $(apiAppName)
        package: '$(Build.ArtifactStagingDirectory)/api.zip'
        startUpCommand: 'node dist/main'

# ==========================================================
# STAGE 3: FRONTEND DEPLOY (Apps/Web)
# ==========================================================
- stage: Frontend
  displayName: 'Deploy Frontend (Web)'
  dependsOn: CheckChanges
  condition: eq(dependencies.CheckChanges.outputs['Check.CheckStep.webChanged'], 'true')
  variables:
    workingDir: 'apps/web'
  jobs:
  - job: BuildAndDeployWeb
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'

    - script: npm ci
      workingDirectory: $(workingDir)
      displayName: 'Install Web Dependencies'

    - script: npm run build
      workingDirectory: $(workingDir)
      displayName: 'Build Next.js'

    # CHANGED: Replaced 'cp' (Linux) with 'Copy-Item' (PowerShell)
    - powershell: |
        # Create destination directories first to avoid errors
        New-Item -ItemType Directory -Force -Path .next/standalone/apps/web/public
        New-Item -ItemType Directory -Force -Path .next/standalone/apps/web/.next/static
        
        # Copy Public folder
        Copy-Item -Path "public\*" -Destination ".next/standalone/apps/web/public" -Recurse -Force
        
        # Copy Static folder
        Copy-Item -Path ".next/static\*" -Destination ".next/standalone/apps/web/.next/static" -Recurse -Force
      workingDirectory: $(workingDir)
      displayName: 'Copy Static Assets'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(workingDir)/.next/standalone/apps/web'
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/web.zip'
        replaceExistingArchive: true

    - task: AzureWebApp@1
      inputs:
        azureSubscription: $(azureSubscription)
        appType: 'webAppLinux'
        appName: $(webAppName)
        package: '$(Build.ArtifactStagingDirectory)/web.zip'
        startUpCommand: 'node server.js'