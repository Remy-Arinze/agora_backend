trigger:
  branches:
    include:
      - dev
      - staging

pool:
  vmImage: 'ubuntu-latest'

variables:
- ${{ if eq(variables['Build.SourceBranchName'], 'dev') }}:
  - group: agora-dev-rg
  - name: apiAppName
    value: 'agora-api-dev'
  - name: azureSubscription
    value: 'agora-current-sc'
  - name: environmentName
    value: 'Dev'

- ${{ if eq(variables['Build.SourceBranchName'], 'staging') }}:
  - group: agora-stg-rg
  - name: apiAppName
    value: 'agora-api-stg'
  - name: azureSubscription
    value: 'agora-current-sc-stg'
  - name: environmentName
    value: 'Staging'

stages:
- stage: Backend
  displayName: 'Deploy Backend'
  jobs:
  - job: BuildAndDeployAPI
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '22.x'          # ✅ Fixed: match Azure Node version
      displayName: 'Install Node.js 22'

    - script: npm ci                  # ✅ ci is faster and cleaner than install
      displayName: 'Install Dependencies'

    - script: |
        echo "=== Environment: $(environmentName) ==="
        echo "=== Deploying Database Migrations ==="
        npx prisma migrate deploy --schema=prisma/schema
        
        echo "=== Regenerating Prisma Client ==="
        npx prisma generate --schema=prisma/schema
      displayName: 'Migrate Database and Update Prisma Client'
      env:
        DB_URL: $(DB_Url)

    - script: |
        echo "=== Building NestJS Application ==="
        npx nest build
        
        if [ ! -f "dist/main.js" ]; then
          echo "##vso[task.logissue type=error]dist/main.js not found"
          exit 1
        fi
      displayName: 'Build NestJS'
      env:
        NODE_OPTIONS: '--max-old-space-size=4096'

    - bash: |
        DEPLOY_DIR=$(Build.SourcesDirectory)/deploy_api
        mkdir -p $DEPLOY_DIR
        cp -r dist $DEPLOY_DIR/
        cp package.json $DEPLOY_DIR/
        cp package-lock.json $DEPLOY_DIR/   # ✅ needed for npm ci on Azure
        if [ -d "prisma" ]; then cp -r prisma $DEPLOY_DIR/; fi
        # ✅ DO NOT copy node_modules — Azure will install them fresh
      displayName: 'Assemble Package'

    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: '$(Build.SourcesDirectory)/deploy_api'
        includeRootFolder: false
        archiveFile: '$(Build.ArtifactStagingDirectory)/api.zip'
        replaceExistingArchive: true
      displayName: 'Create Archive'

    - task: AzureWebApp@1
      inputs:
        azureSubscription: $(azureSubscription)
        appType: 'webAppLinux'
        appName: $(apiAppName)
        package: '$(Build.ArtifactStagingDirectory)/api.zip'
        startUpCommand: 'node dist/main'
      displayName: 'Deploy to $(environmentName)'